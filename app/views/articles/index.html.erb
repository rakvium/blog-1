<body class='general'>
<p> Our blog has <%= pluralize(Article.public_count, "article") %> and counting! </p>





<%= search_form_for @q, remote: true do |f| %>
  <%= f.search_field :title_or_body_cont, placeholder: t('search for articles'), class: "form-control"  %>
  <%= f.submit%>
<% end %>

<% if user_signed_in? && current_user.role == "admin" %>
  <%= render "partials/allarticles", articles: @articles%>
<%else%>
   <%=render partial: 'articles'%>
<%end%>


<body>
    <%= yield %>
</body>
</body>

