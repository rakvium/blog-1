<%= form_with(model: [article, comment]) do |f| %>
  <div >
    <%= f.text_area :body, style: "width: 100%; " %>
    <%= f.submit submit_tag, class: "btn btn-primary mt-1;", style: "width:20%;  font-size: 20px;" %>
  </div>
<% end %>