<%= render @phone %>

<div>

  <%= link_to t("Edit this phone"), edit_user_phone_path(@user,@phone),  class: "btn btn-outline-dark" %> 

  <%= link_to t("Delete phone"), [@phone.user, @phone], data: {
                  turbo_method: :delete,
                  turbo_confirm: t("Are you sure?")
                },  class: "btn btn-outline-danger" %>

  <%= link_to t("Back to contacts"),  user_phones_path(@user), class: "btn btn-outline-dark"%>
</div>
